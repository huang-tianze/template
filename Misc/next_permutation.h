#include <algorithm>
#include <string>
using namespace std;
int main() {
    string combination = "0000000111"; // 从字典序最小的开始遍历
    do {
        // bruh
    } while (next_permutation(combination.begin(), combination.end()));
}